<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Основные цвета (можно использовать как переменные) -->
    <SolidColorBrush x:Key="PrimaryColor" Color="#FF0066CC" />
    <!-- синий основной -->
    <SolidColorBrush x:Key="PrimaryLight" Color="#FF3399FF" />
    <SolidColorBrush x:Key="BackgroundColor" Color="#FFF5F7FA" />
    <SolidColorBrush x:Key="TextColor" Color="#FF1A1A1A" />
    <SolidColorBrush x:Key="BorderColor" Color="#FFD0D7E0" />

    <!-- Шрифты -->
    <FontFamily x:Key="DefaultFont">Segoe UI</FontFamily>
    <FontFamily x:Key="HeaderFont">Segoe UI Semibold</FontFamily>

    <!-- Общий стиль для всех кнопок -->
    <Style x:Key="PageButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource PrimaryColor}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="12,6" />
        <Setter Property="Margin" Value="6" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="{StaticResource DefaultFont}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="4"
                            BorderThickness="0">
                        <ContentPresenter HorizontalAlignment="Center"
                                         VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{StaticResource PrimaryLight}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#FF0052A3" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для заголовков -->
    <Style x:Key="PageHeaderText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource PrimaryColor}" />
        <Setter Property="Margin" Value="0,10,0,15" />
    </Style>

    <!-- Стиль для DataGrid -->
    <Style x:Key="PageDataGrid" TargetType="DataGrid">
        <Setter Property="Background" Value="White" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderColor}" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="#FFE0E0E0" />
        <Setter Property="RowBackground" Value="White" />
        <Setter Property="AlternatingRowBackground" Value="#FAFAFA" />
        <Setter Property="Margin" Value="0,10" />
    </Style>

    <!-- Стиль для логотипа -->
    <Style x:Key="CompanyLogoStyle" TargetType="Image">
        <Setter Property="Height" Value="48" />
        <Setter Property="Margin" Value="12,8" />
        <Setter Property="HorizontalAlignment" Value="Left" />
    </Style>
    <Style x:Key="PageTitle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="PageComboBox" TargetType="ComboBox">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Margin" Value="5"/>
    </Style>

</ResourceDictionary>